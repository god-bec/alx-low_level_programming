#include "main.h"
/**
 * add - adds the integers and return the result
 * @a: int to be added to be
 * @b: int to be added to a
 * Return: sum of a and b
 *
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
